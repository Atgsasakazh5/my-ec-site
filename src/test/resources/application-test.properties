# ===============================================================
# For CI/CD Test Environment (MySQL on Docker)
# ===============================================================

# docker-compose.ymlの環境変数を参照してMySQLに接続
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================================================
# JPA / HIBERNATE for MySQL
# ===============================================================
# アプリケーション起動時にDBを初期化し、終了時に削除する
spring.jpa.hibernate.ddl-auto=create-drop

# MySQL用のSQL方言(dialect)を指定する
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# 実行されるSQLをコンソールに表示する
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# data.sqlによる初期化は行わない
spring.sql.init.mode=never

# ===============================================================
# Test Properties
# ===============================================================
app.JWT_SECRET=dGVzdFNlY3JldEtleUZvclRlc3RpbmdQdXJwb3Nlc0FuZFNlY3VyaXR5MTIzNDU2Nzg5MA==
app.JWT_EXPIRATION_MS=3600000