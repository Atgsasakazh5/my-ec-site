# ===============================================================
# For CI/CD Test Environment (MySQL on Docker)
# ===============================================================
# H2データベースの自動設定を無効にする
# H2のテストデータベース自動設定だけを無効化する
spring.autoconfigure.exclude=org.springframework.boot.test.autoconfigure.jdbc.TestDatabaseAutoConfiguration
spring.test.database.replace=NONE
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=ALWAYS
spring.sql.init.platform=mysql
# ===============================================================
# Test Properties
# ===============================================================
app.JWT_SECRET=dGVzdFNlY3JldEtleUZvclRlc3RpbmdQdXJwb3Nlc0FuZFNlY3VyaXR5MTIzNDU2Nzg5MA==
app.JWT_EXPIRATION_MS=3600000
# Stripe Secret Key
stripe.api.secret-key=${STRIPE_SECRET_KEY}
# Stripe??????????URL
app.stripe.return-url=${STRIPE_RETURN_URL}